diff --git a/node_modules/expo-splash-screen/android/src/main/java/expo/modules/splashscreen/SplashScreenManager.kt b/node_modules/expo-splash-screen/android/src/main/java/expo/modules/splashscreen/SplashScreenManager.kt
index 4a9cb0a..af3941e 100644
--- a/node_modules/expo-splash-screen/android/src/main/java/expo/modules/splashscreen/SplashScreenManager.kt
+++ b/node_modules/expo-splash-screen/android/src/main/java/expo/modules/splashscreen/SplashScreenManager.kt
@@ -35,19 +35,12 @@ object SplashScreenManager {
 
     splashScreen.setOnExitAnimationListener { splashScreenViewProvider ->
       val splashScreenView = splashScreenViewProvider.view
-      splashScreenView
-        .animate()
-        .setDuration(duration)
-        .alpha(0.0f)
-        .setInterpolator(AccelerateInterpolator())
-        .withEndAction {
-          if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
-            splashScreenViewProvider.remove()
-          } else {
-            // Avoid calling applyThemesSystemBarAppearance
-            (splashScreenView as SplashScreenView).remove()
-          }
-        }.start()
+      if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
+        splashScreenViewProvider.remove()
+      } else {
+        // Avoid calling applyThemesSystemBarAppearance
+        (splashScreenView as SplashScreenView).remove()
+      }
     }
   }
 
